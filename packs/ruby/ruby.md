# Ruby

RuboCop clean. Symbols for identifiers, frozen string literals. Never monkeypatch
core classes from app code. Prefer composition over metaprogramming magic.
`bundle audit` in CI; keep gems updated.
