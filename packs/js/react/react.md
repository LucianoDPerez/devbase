# React

Functional components + hooks. Stable keys, never array indexes for dynamic lists.
Effects with cleanup; no fetching in effects without a data layer (query lib or
loader). Business logic in hooks/services, components render. No prop drilling
past two levels — context or store.
