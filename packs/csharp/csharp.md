# C#

Nullable reference types on, warnings as errors in CI. Async all the way —
no `.Result` or `.Wait()` (deadlocks). `IDisposable` with `using`. LINQ over
manual loops when readable. `dotnet format` clean; no regions hiding design smells.
