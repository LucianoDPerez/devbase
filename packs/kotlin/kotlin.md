---
level: heuristic
---

# Kotlin

Null safety is the point: `?.` and `?:`, no `!!` without a justifying comment.
Data classes for data, sealed hierarchies for state. Coroutines with structured
concurrency — no `GlobalScope`. Immutable `val` by default.