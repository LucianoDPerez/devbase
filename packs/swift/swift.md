---
level: heuristic
---

# Swift

`guard let` over nested `if let`; no force unwrap in production paths. Value
types (`struct`) by default, classes for identity. Swift Concurrency
async/await, actors for shared mutable state. No `print` in shipped code.