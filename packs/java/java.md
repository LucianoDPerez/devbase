# Java

Prefer records and sealed types for data. Streams for readable transforms, loops
when performance matters. Never return null — use Optional or empty collections.
No `System.out` in production code; use the project logger. Build with Maven or
Gradle; keep dependencies pinned and audited.
