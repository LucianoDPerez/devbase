# Spring Boot

Thin controllers, logic in services. Constructor injection, never field injection.
Validate at boundaries with `@Valid`. `@Transactional` at the service layer, not
in controllers. Externalize config per profile; actuator secured, never public.
