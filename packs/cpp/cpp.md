---
level: heuristic
---

# C++

Modern C++: RAII, smart pointers, no raw `new`/`delete` in app code.
Const-correctness everywhere. No `using namespace std` in headers.
`clang-tidy` and `clang-format` clean; sanitizers in CI.